The image in the markdown page is too large that it overflows the container.
The markdown content is not responsive and does not adjust to different screen sizes.
Consider moving the sidebar drawer to the left side of the screen.
The go-back button and sidebar drawer button should be sticky and always visible when scrolling through the content.
Create a new file for the TOC part.
Articles don't have TOC.
If the headings contain Chinese characters, the TOC will not jump when clicking. Also if I click on a TOC item, the URL should update with the corresponding hash for better navigation and sharing. (e.g. #heading-id)